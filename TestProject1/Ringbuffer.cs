
// a ringbuffer with the given size
public class Ringbuffer<T>
{
    public Ringbuffer(int maxSize)
    {
    }

    /// <summary>
    /// adds the given element to the `end` of the ring
    /// </summary>
    public void Add(T element)
    {
    }

    /// <summary>
    /// get and remove the oldest element from the index
    /// </summary>
    public T Remove()
    {
        return default(T);
    }

    /// <summary>
    /// get the number of elements in the ringbuffer
    /// </summary>
    /// <returns></returns>
    public int Count()
    {
        return 0;
    }
}